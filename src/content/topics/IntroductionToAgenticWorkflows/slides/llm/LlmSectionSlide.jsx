/* Section divider: the title carries the section, so no kicker. */

function LlmSectionSlide() {
  return null
}

LlmSectionSlide.meta = {
  kind: 'title',
  title: 'LLM',
  subtitle: 'Creating reasoning',
  section: 'llm',
  notes: 'The layer everything else is built on: a model that predicts, and the reasoning that falls out of it.',
}

export default LlmSectionSlide
