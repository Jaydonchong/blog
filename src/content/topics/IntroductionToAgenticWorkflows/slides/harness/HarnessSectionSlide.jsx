/* Section divider: the title carries the section, so no kicker. */

function HarnessSectionSlide() {
  return null
}

HarnessSectionSlide.meta = {
  kind: 'title',
  title: 'Harness',
  subtitle: 'Packaging environment',
  section: 'harness',
  notes: 'The harness is what holds the prompt, the context, and the tools together as one runnable thing.',
}

export default HarnessSectionSlide
